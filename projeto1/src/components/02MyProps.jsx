const MyProps = (props) => {
    return (
        <div>
            <h1>Meu nome eh: {props.nome}</h1>
            <h1>Meu curso eh: {props.curso}</h1>
        </div>
    );
}

export default MyProps;
